<div class="navbar navbar-default navbar-static-top">
    <div class="container">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <span>
            <%= link_to ("<i class='fa fa-rocket'></i> " +'Project Management App').html_safe, root_path, class: "navbar-brand" %>
        </span>
        <div class="navbar-collapse collapse navbar-responsive-collapse">
            <ul class="nav navbar-nav">
                <% if current_user && current_user.admin? %>
                <li>
                    <%= link_to "<i class='fa fa-users'></i> Add Members".html_safe, new_member_path,  class: 'navbar-link btn btn-primary' %>
                </li>
                <li>
                    <%= link_to "<i class='fa fa-briefcase'></i> Change Plan".html_safe, edit_plan_path, class: 'navbar-link btn btn-primary' %>
                </li>
                <% end %>    
            </ul>
            <ul class="nav navbar-right col-md-4">
                <% if current_user %>
                
                <% end %> 
            </ul>
        </div>
    </div>
</div>